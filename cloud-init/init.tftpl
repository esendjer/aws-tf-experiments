#cloud-config
ssh_authorized_keys:
%{ for ssh_key in ssh_keys ~}
  - ${ssh_key}
%{ endfor ~}

package_update: true
package_upgrade: true

packages:
%{ for package in packages ~}
  - ${package}
%{ endfor ~}

${additional_cloud_init_content}